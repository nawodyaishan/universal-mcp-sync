version: 2

project_name: usync

before:
  hooks:
    - go mod tidy

builds:
  - id: usync
    main: ./cmd/usync
    binary: usync
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - >-
        -s -w
        -X github.com/nawodyaishan/universal-mcp-sync/pkg/version.Version={{ .Version }}
        -X github.com/nawodyaishan/universal-mcp-sync/pkg/version.Commit={{ .ShortCommit }}
        -X github.com/nawodyaishan/universal-mcp-sync/pkg/version.Date={{ .Date }}
        -X github.com/nawodyaishan/universal-mcp-sync/pkg/version.GoVersion={{ .Env.GOVERSION }}

archives:
  - id: default
    formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

brews:
  - repository:
      owner: nawodyaishan
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/nawodyaishan/universal-mcp-sync"
    description: "macOS-first developer utility for universal MCP configuration sync"
    license: "MIT"

# nFPM for deb/rpm packages (bonus)
nfpms:
  - id: packages
    package_name: usync
    maintainer: nawodyaishan <nawodya.dev@gmail.com>
    vendor: nawodyaishan
    homepage: https://github.com/nawodyaishan/universal-mcp-sync
    description: "macOS-first developer utility for universal MCP configuration sync"

    license: MIT
    formats:
      - deb
      - rpm
